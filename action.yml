#action.yml

name: "SilverBass - Music Playlist Generator"
description: "Generates a weekly music playlist and updates GitHub Pages"
inputs:
  theme:
    description: "Theme for the playlist (optional)"
    required: false
runs:
  using: "docker"
  image: "Dockerfile"
